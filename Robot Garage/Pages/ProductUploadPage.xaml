<Page x:Class="Robot_Garage.ProductUploadPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      xmlns:local="clr-namespace:Robot_Garage"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductUploadPage">

    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Product Name -->
            <Label Grid.Row="0" Grid.Column="0" Content="Product Name:" FontSize="14" VerticalAlignment="Center" Margin="0,0,0,10" />
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtName" Margin="10,0,0,10" />

            <!-- Description -->
            <Label Grid.Row="1" Grid.Column="0" Content="Description:" FontSize="14" VerticalAlignment="Center" Margin="0,0,0,10" />
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtDescription" AcceptsReturn="True" TextWrapping="Wrap" Margin="10,0,0,10" />

            <!-- Price -->
            <Label Grid.Row="2" Grid.Column="0" Content="Price:" FontSize="14" VerticalAlignment="Center" Margin="0,0,0,10" />
            <xctk:IntegerUpDown Grid.Row="2" Grid.Column="1" x:Name="txtPrice" Margin="10,0,0,10" />

            <!-- Condition -->
            <Label Grid.Row="3" Grid.Column="0" Content="Condition:" FontSize="14" VerticalAlignment="Center" Margin="0,0,0,10" />
            <xctk:DropDownButton Grid.Row="3" Grid.Column="1" x:Name="txtCondition"  Margin="10,0,0,10" Content="Select Condition"
                                 Click="txtCondition_Click">
                <xctk:DropDownButton.DropDownContent>
                    <ListBox x:Name="ConditionListBox" Visibility="Collapsed" SelectionChanged="ConditionListBox_SelectionChanged">
                        <!-- Items will be populated dynamically -->
                    </ListBox>
                </xctk:DropDownButton.DropDownContent>
            </xctk:DropDownButton>

            <!-- Image Upload -->
            <Label Grid.Row="4" Grid.Column="0" Content="Product Image:" FontSize="14" VerticalAlignment="Center" Margin="0,0,0,10" />
            <Button x:Name="ImgButton" Grid.Row="4" Grid.Column="1" Content="Select Image" Click="SelectImage_Click" Margin="10,0,0,10" />
            <Image x:Name="ImgPreview" Visibility="Collapsed" Grid.Row="4" Grid.Column="1" Source="/assets/images/Hammer.png" VerticalAlignment="Top" HorizontalAlignment="Left" Height="250"/>

            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="txtImagePath" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,10" />

        </Grid>
        
        <!-- Submit Button -->
        <TextBlock x:Name="txtSuccess" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                   Margin="0,0,0,40" Foreground="Green" FontSize="16"/>
        <Button Content="Upload" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                Click="Upload_Click" Margin="10" Padding="25, 5" />
    </Grid>
</Page>
